diff --git a/node_modules/@sentry/node/dist/handlers.js b/node_modules/@sentry/node/dist/handlers.js
index 4e8f18c..4e709a9 100644
--- a/node_modules/@sentry/node/dist/handlers.js
+++ b/node_modules/@sentry/node/dist/handlers.js
@@ -289,7 +289,8 @@ function requestHandler(options) {
         var local = domain.create();
         local.add(req);
         local.add(res);
-        local.on('error', next);
+        // Kirill: skipping this on-error call to avoid breaking user-requests with `void` functions
+        // local.on('error', next);
         local.run(function () {
             var currentHub = core_1.getCurrentHub();
             currentHub.configureScope(function (scope) {
